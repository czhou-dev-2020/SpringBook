package com.rillsoft.springbookadmin.Token.repository;

// import com.rillsoft.springbookadmin.Token.data.AuthUserTokens;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

// added by crane zhou on 2021.11.16
// Refer to:
//      https://docs.spring.io/spring-data/jpa/docs/current/api/org/springframework/data/
//          jpa/repository/JpaRepository.html
//

/*
@Repository
public interface AuthUserTokensRepository extends JpaRepository<AuthUserTokens, Long> {
//    AuthUserTokens findByTokenName(final String strTokenName);
//    Long removeByTokenName(String strTokenName);
}
*/

